# 通信
## 硬件：设备主要的通信硬件有两种，串口和TCP。
##通信协议：而主流的Modbus、Tcp、S7等都是基于上面硬件的一些规则协议。
	单片机：我们自己的单片机就是只提供了硬件串口，单片机串口和主机串口硬件连接后，PC发送数据单片机接收处理，这里就是双方自己的约定，没有使用协议。而我们也可以跟据modbus的协议，将接收和发送的消息数据安装modbus的规格处理发送。
	PLC：像永宏PLC有自己的协议，而且不需要自己写，都是封装好了的，我们PC的COM口和PLC的串口连接上了，PC通过串口安装指定的协议发送和接收就能和PLC交互了。